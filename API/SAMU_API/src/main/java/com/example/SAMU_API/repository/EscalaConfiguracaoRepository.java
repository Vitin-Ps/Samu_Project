package com.example.SAMU_API.repository;

import com.example.SAMU_API.domain.escala.configuracao.EscalaConfiguracao;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EscalaConfiguracaoRepository extends JpaRepository<EscalaConfiguracao, Long> {
}
