package com.example.SAMU_API.repository;

import com.example.SAMU_API.domain.escala.personalizada.EscalaPersonalizada;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EscalaPersonalizadaRepository extends JpaRepository<EscalaPersonalizada, Long> {
}
